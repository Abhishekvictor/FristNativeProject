import { Text, Button, View, Alert, } from 'react-native'

const Btn = () => {
    return (

        <View>
            <Button title='Update Name' onPress={testbtn} color={'red'}
            />
        </View>
    )
}

export default Btn 